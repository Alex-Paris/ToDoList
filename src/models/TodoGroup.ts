class TodoGroup {
  name: string;

  totalItems: number;

  doneItems: number;

  constructor () {
    this.name = '';

    this.totalItems = 0;

    this.doneItems = 0;
  }
}

export default TodoGroup;
